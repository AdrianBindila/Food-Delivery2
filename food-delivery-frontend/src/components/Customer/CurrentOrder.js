//after finalizing cart order, move here
//use an observer pattern to see if the admin changes order status
function CurrentOrder() {
    return(<div>
        <h1>CurrentOrder</h1>
    </div>);
}
export default CurrentOrder;